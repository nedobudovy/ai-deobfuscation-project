# Python dependencies for the AI-assisted deobfuscation pipeline.
#
#   httpx            — API clients for all LLM providers (models_api.py)
#   pyyaml           — config loading (config.yaml)
#   streamlit        — web demo UI (app.py)
#   lizard           — cyclomatic complexity, used by the S axis of the rubric
#   tree-sitter      — C AST parsing, used by the S and R axes (deobf_evaluator.py)
#   tree-sitter-c    — C grammar bindings for tree-sitter

httpx>=0.27.0
pyyaml>=6.0
streamlit>=1.30
lizard>=1.17.10
tree-sitter>=0.22,<0.26
tree-sitter-c>=0.22,<0.26
